[project]
name = "remote-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Raj Gehlot", email = "rajgehlot2007@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=4.0.3",
]

[project.scripts]
remote-mcp = "remote_mcp:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
